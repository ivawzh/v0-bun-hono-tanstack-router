# Solo Unicorn Server Environment Variables

# Basic server configuration
PORT=8500

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/solo_unicorn

# Claude Code UI Integration
CLAUDE_CODE_UI_URL=http://localhost:8501

CLAUDE_CODE_UI_AUTH_TOKEN=your-secure-agent-token-here

# Authorization Configuration
# Comma-separated list of email addresses authorized to access the application
# If empty or undefined, all authenticated users will be allowed (not recommended for production)
AUTHORIZED_EMAILS=admin@example.com,developer@startup.io

# MCP Server is now integrated into the main server on PORT (8500)

# CORS Configuration
CORS_ORIGIN=http://localhost:8302
